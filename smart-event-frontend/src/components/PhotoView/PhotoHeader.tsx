import { AppBar, Toolbar } from '@mui/material'
import React from 'react'

const PhotoHeader = () => {
  return (
    <AppBar>
        <Toolbar></Toolbar>
    </AppBar>
  )
}

export default PhotoHeader
